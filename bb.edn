{:paths ["src" "test"]
 :tasks
 {test {:doc "Run yakushi cljc actor boundary tests"
        :task (do
                (require 'clojure.test
                         'yakushi.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'yakushi.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
